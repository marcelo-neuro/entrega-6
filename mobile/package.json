{
  "name": "pagamento-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "VITE_API_BASE=http://localhost:8080 vite",
    "build": "vite build",
    "preview": "VITE_API_BASE=http://localhost:8080 vite preview --port 4173"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
  "@vitejs/plugin-react": "4.0.0",
  "vite": "^4.2.0"
  }
}
